#!/bin/bash
BASEDIR='/home/mattias/switch/payloads' #hard-code because it runs in an udev context
#PAYLOAD="${1:-fusee-primary-0.12.bin}"
PAYLOAD="${1:-hekate_ctcaer_5.3.0.bin}"

fusee-launcher "${BASEDIR}/${PAYLOAD}"
